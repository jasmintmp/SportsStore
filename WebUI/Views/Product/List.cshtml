@using Domain.Entities
@model IEnumerable<Product>


@{
    ViewBag.Title = "List";
}

<h2>List</h2>
@foreach (var p in Model)
{
<div>
    @p.Name
    @p.Description
    <h4>@p.Price.ToString("c")</h4>
</div>
}
